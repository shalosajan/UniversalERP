[project]
name = "universalerp"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff.lint]
# Ignore RUF012 because Django uses mutable lists heavily for choices, meta fields, and migrations.
ignore = ["RUF012"]